package com.shagalalab.qqkeyboard.keyboard.theme

import androidx.compose.ui.unit.dp

object KeyboardDimensions {
    val rowGap = 8.dp                  // spacedBy in column
    val gridHorizontalPadding = 2.dp   // horizontal padding around key grid (Box + Column)
    val gridVerticalPadding = 4.dp     // vertical padding around key grid (Box + Column)
    val keyHorizontalPadding = 2.dp    // visual gap between keys
    val suggestionStripHeight = 40.dp
}
